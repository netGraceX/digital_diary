spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/diaries
    username: netgrace
    password: 22fall19
  jpa:
  hibernate:
    ddl-auto: create-drop
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
    format_sql: true
  show-sql: true
